# Random share tokens

Dongmodel uses random share tokens for public Share Card, Group Share Card, and Wanted Share links instead of predictable URLs based on owner handles or item names. Public links must be easy to open from social platforms, but they should not make an Owner's shared items easy to enumerate. Human-readable owner handles remain useful for logged-in Owner Gallery lookup, while share links use non-guessable tokens.
