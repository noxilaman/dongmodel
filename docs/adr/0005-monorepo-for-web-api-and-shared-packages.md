# Monorepo for web, API, and shared packages

Dongmodel keeps the Next.js frontend, NestJS backend API, and shared packages in one pnpm workspace monorepo. The frontend and backend remain separate applications with separate responsibilities, but the monorepo keeps domain language, API contracts, Zod validation schemas, shared types, and documentation moving together as the product evolves.
