# REST API between web and backend

Dongmodel uses a REST API between the Next.js frontend and NestJS backend. The product is resource-oriented around Modong, Wanted Items, groups, shares, galleries, admin-managed kinds, and uploads, so REST keeps the first version straightforward. GraphQL can be revisited later if clients need more flexible cross-resource querying than the REST API naturally supports.
